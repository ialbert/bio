# Documentation

## Installation

Install `bookdown` and `serv` with conda

    conda install r-bookdown r-servr

Install the bio package either as end user:

    pip install bio

or as a developer:

    python setup.py develop

## Generate the html documenation

    make

