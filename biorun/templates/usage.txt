Bioinformatics utilities: {VERSION}

Usage: bio COMMAND

Data commands

   {{ "%-10s" | format(FETCH) }}  - downloads GenBank/EMBL data into cache
   {{ "%-10s" | format(VIEW)  }}  - converts/extracts information from GenBank/EMBL data

Action commands

    {{ "%-10s" | format(ALIGN) }} - aligns sequences with different algorithms

Get more help on each command with:

    bio COMMAND -h

Examples:

    bio view AF086833

